rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // PUBLIC READ ACCESS TO FIX AUTH ERRORS
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
